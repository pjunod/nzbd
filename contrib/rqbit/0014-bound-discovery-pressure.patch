diff --git a/crates/dht/src/dht.rs b/crates/dht/src/dht.rs
index c255e75e..1c3f18da 100644
--- a/crates/dht/src/dht.rs
+++ b/crates/dht/src/dht.rs
@@ -39,7 +39,7 @@ use parking_lot::RwLock;
 use serde::Serialize;
 use tokio::{
     net::UdpSocket,
-    sync::mpsc::{channel, unbounded_channel, Sender, UnboundedReceiver, UnboundedSender},
+    sync::mpsc::{channel, error::TrySendError, Receiver, Sender},
 };
 
 use tokio_util::sync::CancellationToken;
@@ -64,6 +64,40 @@ pub struct WorkerSendRequest {
     addr: SocketAddr,
 }
 
+const MAX_PENDING_DHT_SENDS: usize = 256;
+const MAX_PENDING_DISCOVERED_PEERS: usize = 256;
+const MAX_PENDING_RECURSIVE_NODES: usize = 256;
+const MAX_CONCURRENT_RECURSIVE_REQUESTS: usize = 32;
+const MAX_PENDING_MAINTENANCE_REQUESTS: usize = 256;
+const MAX_CONCURRENT_MAINTENANCE_REQUESTS: usize = 32;
+const MAX_CONCURRENT_BOOTSTRAPS: usize = 8;
+
+type RecursiveNode = (Option<Id20>, SocketAddr, usize);
+
+fn worker_send_channel() -> (Sender<WorkerSendRequest>, Receiver<WorkerSendRequest>) {
+    channel(MAX_PENDING_DHT_SENDS)
+}
+
+fn discovered_peer_channel() -> (Sender<SocketAddr>, Receiver<SocketAddr>) {
+    channel(MAX_PENDING_DISCOVERED_PEERS)
+}
+
+fn recursive_node_channel() -> (Sender<RecursiveNode>, Receiver<RecursiveNode>) {
+    channel(MAX_PENDING_RECURSIVE_NODES)
+}
+
+fn maintenance_channel<T>() -> (Sender<T>, Receiver<T>) {
+    channel(MAX_PENDING_MAINTENANCE_REQUESTS)
+}
+
+fn can_schedule_recursive_request(active: usize) -> bool {
+    active < MAX_CONCURRENT_RECURSIVE_REQUESTS
+}
+
+fn can_schedule_maintenance_request(active: usize) -> bool {
+    active < MAX_CONCURRENT_MAINTENANCE_REQUESTS
+}
+
 #[derive(Debug)]
 struct MaybeUsefulNode {
     id: Id20,
@@ -144,7 +178,7 @@ impl RecursiveRequestCallbacks for RecursiveRequestCallbacksGetPeers {
             port: announce_port,
         });
 
-        let _ = req.dht.worker_sender.send(WorkerSendRequest {
+        let _ = req.dht.try_send_worker(WorkerSendRequest {
             our_tid: Some(tid),
             message,
             addr,
@@ -187,20 +221,20 @@ struct RecursiveRequest<C: RecursiveRequestCallbacks> {
     request: Request,
     dht: Arc<DhtState>,
     useful_nodes: RwLock<Vec<MaybeUsefulNode>>,
-    peer_tx: tokio::sync::mpsc::UnboundedSender<SocketAddr>,
-    node_tx: tokio::sync::mpsc::UnboundedSender<(Option<Id20>, SocketAddr, usize)>,
+    peer_tx: Sender<SocketAddr>,
+    node_tx: Sender<RecursiveNode>,
     callbacks: C,
 }
 
 pub struct RequestPeersStream {
-    rx: tokio::sync::mpsc::UnboundedReceiver<SocketAddr>,
+    rx: Receiver<SocketAddr>,
     cancel_join_handle: tokio::task::JoinHandle<()>,
 }
 
 impl RequestPeersStream {
     fn new(dht: Arc<DhtState>, info_hash: Id20, announce_port: Option<u16>) -> Self {
-        let (peer_tx, peer_rx) = unbounded_channel();
-        let (node_tx, node_rx) = unbounded_channel();
+        let (peer_tx, peer_rx) = discovered_peer_channel();
+        let (node_tx, node_rx) = recursive_node_channel();
         let rp = Arc::new(RecursiveRequest {
             max_depth: 4,
             info_hash,
@@ -250,7 +284,7 @@ impl RecursiveRequest<RecursiveRequestCallbacksFindNodes> {
         target: Id20,
         addrs: impl Iterator<Item = SocketAddr>,
     ) -> anyhow::Result<()> {
-        let (node_tx, mut node_rx) = unbounded_channel();
+        let (node_tx, mut node_rx) = recursive_node_channel();
         let req = RecursiveRequest {
             max_depth: 4,
             info_hash: target,
@@ -258,7 +292,7 @@ impl RecursiveRequest<RecursiveRequestCallbacksFindNodes> {
             dht,
             useful_nodes_limit: 32,
             useful_nodes: RwLock::new(Vec::new()),
-            peer_tx: unbounded_channel().0,
+            peer_tx: discovered_peer_channel().0,
             node_tx,
             callbacks: RecursiveRequestCallbacksFindNodes {},
         };
@@ -276,33 +310,33 @@ impl RecursiveRequest<RecursiveRequestCallbacksFindNodes> {
                 ))
         };
 
-        let mut futs = FuturesUnordered::new();
-
         let mut initial_addrs = 0;
         for addr in addrs {
-            futs.push(request_one(None, addr, 0));
-            initial_addrs += 1;
+            match req.node_tx.try_send((None, addr, 0)) {
+                Ok(()) => initial_addrs += 1,
+                Err(TrySendError::Full(_)) => break,
+                Err(TrySendError::Closed(_)) => bail!("recursive request queue closed"),
+            }
         }
 
+        let mut futs = FuturesUnordered::new();
         let mut successes = 0;
         let mut errors = 0;
 
         loop {
+            if futs.is_empty() && node_rx.is_empty() {
+                break;
+            }
             tokio::select! {
                 biased;
 
-                r = node_rx.recv() => {
-                    let (id, addr, depth) = r.unwrap();
+                r = node_rx.recv(), if can_schedule_recursive_request(futs.len()) => {
+                    let Some((id, addr, depth)) = r else {
+                        break;
+                    };
                     futs.push(request_one(id, addr, depth))
                 },
-                f = futs.next() => {
-                    let f = match f {
-                        Some(f) => f,
-                        None => {
-                            // find_node recursion finished.
-                            break;
-                        }
-                    };
+                Some(f) = futs.next(), if !futs.is_empty() => {
                     if f.is_ok() {
                         successes += 1;
                     } else {
@@ -324,7 +358,7 @@ impl RecursiveRequest<RecursiveRequestCallbacksFindNodes> {
 impl RecursiveRequest<RecursiveRequestCallbacksGetPeers> {
     fn request_peers_forever(
         self: &Arc<Self>,
-        mut node_rx: tokio::sync::mpsc::UnboundedReceiver<(Option<Id20>, SocketAddr, usize)>,
+        mut node_rx: Receiver<RecursiveNode>,
     ) -> tokio::task::JoinHandle<()> {
         let this = self.clone();
         spawn(
@@ -356,7 +390,7 @@ impl RecursiveRequest<RecursiveRequestCallbacksGetPeers> {
                 let mut futs = FuturesUnordered::new();
                 loop {
                     tokio::select! {
-                        addr = node_rx.recv() => {
+                        addr = node_rx.recv(), if can_schedule_recursive_request(futs.len()) => {
                             let (id, addr, depth) = addr.unwrap();
                             futs.push(
                                 this.request_one(id, addr, depth)
@@ -385,8 +419,11 @@ impl RecursiveRequest<RecursiveRequestCallbacksGetPeers> {
             .map(|n| (n.id(), n.addr()))
             .take(8)
         {
-            count += 1;
-            self.node_tx.send((Some(id), addr, 0))?;
+            match self.node_tx.try_send((Some(id), addr, 0)) {
+                Ok(()) => count += 1,
+                Err(TrySendError::Full(_)) => break,
+                Err(TrySendError::Closed(_)) => bail!("recursive request queue closed"),
+            }
         }
         Ok(count)
     }
@@ -403,6 +440,7 @@ impl<C: RecursiveRequestCallbacks> RecursiveRequest<C> {
             self.callbacks.on_request_start(self, id, addr);
         }
 
+        // Send once: callbacks and traversal must consume the same response.
         let response = self
             .dht
             .request(self.request.clone(), addr)
@@ -414,7 +452,7 @@ impl<C: RecursiveRequestCallbacks> RecursiveRequest<C> {
             self.callbacks.on_request_end(self, id, addr, &response);
         }
 
-        let response = match self.dht.request(self.request.clone(), addr).await {
+        let response = match response {
             Ok(ResponseOrError::Response(r)) => r,
             Ok(ResponseOrError::Error(e)) => bail!("error response: {:?}", e),
             Err(e) => {
@@ -425,7 +463,10 @@ impl<C: RecursiveRequestCallbacks> RecursiveRequest<C> {
 
         if let Some(peers) = response.values {
             for peer in peers {
-                self.peer_tx.send(SocketAddr::V4(peer.addr))?;
+                self.peer_tx
+                    .send(SocketAddr::V4(peer.addr))
+                    .await
+                    .context("discovered peer receiver closed")?;
             }
         }
 
@@ -442,7 +483,13 @@ impl<C: RecursiveRequestCallbacks> RecursiveRequest<C> {
                     self.max_depth
                 );
                 if should_request {
-                    self.node_tx.send((Some(node.id), addr, depth + 1))?;
+                    match self.node_tx.try_send((Some(node.id), addr, depth + 1)) {
+                        Ok(()) => {}
+                        Err(TrySendError::Full(_)) => {
+                            trace!("recursive request queue is full, dropping {addr}")
+                        }
+                        Err(TrySendError::Closed(_)) => bail!("recursive request queue closed"),
+                    }
                 }
             }
         }
@@ -545,7 +592,7 @@ pub struct DhtState {
     // Sending requests to the worker.
     rate_limiter: RateLimiter,
     // This is to send raw messages
-    worker_sender: UnboundedSender<WorkerSendRequest>,
+    worker_sender: Sender<WorkerSendRequest>,
 
     cancellation_token: CancellationToken,
 
@@ -555,7 +602,7 @@ pub struct DhtState {
 impl DhtState {
     fn new_internal(
         id: Id20,
-        sender: UnboundedSender<WorkerSendRequest>,
+        sender: Sender<WorkerSendRequest>,
         routing_table: Option<RoutingTable>,
         listen_addr: SocketAddr,
         peer_store: PeerStore,
@@ -575,25 +622,35 @@ impl DhtState {
         }
     }
 
+    fn try_send_worker(&self, request: WorkerSendRequest) -> anyhow::Result<bool> {
+        match self.worker_sender.try_send(request) {
+            Ok(()) => Ok(true),
+            Err(TrySendError::Full(_)) => {
+                debug!("DHT send queue is full, dropping untracked datagram");
+                Ok(false)
+            }
+            Err(TrySendError::Closed(_)) => bail!("DHT worker is closed"),
+        }
+    }
+
     async fn request(&self, request: Request, addr: SocketAddr) -> anyhow::Result<ResponseOrError> {
         self.rate_limiter.acquire_one().await;
+        let permit = self
+            .worker_sender
+            .reserve()
+            .await
+            .context("DHT worker is closed")?;
         let (tid, message) = self.create_request(request);
         let key = (tid, addr);
         let (tx, rx) = tokio::sync::oneshot::channel();
         self.inflight_by_transaction_id
             .insert(key, OutstandingRequest { done: tx });
         trace!("sending {message:?}");
-        match self.worker_sender.send(WorkerSendRequest {
+        permit.send(WorkerSendRequest {
             our_tid: Some(tid),
             message,
             addr,
-        }) {
-            Ok(_) => {}
-            Err(e) => {
-                self.inflight_by_transaction_id.remove(&key);
-                return Err(e.into());
-            }
-        };
+        });
         match tokio::time::timeout(RESPONSE_TIMEOUT, rx).await {
             Ok(Ok(r)) => r.map(|r| {
                 trace!("received {r:?}");
@@ -735,7 +792,7 @@ impl DhtState {
                     }),
                 };
                 self.routing_table.write().mark_last_query(&req.id);
-                self.worker_sender.send(WorkerSendRequest {
+                let _ = self.try_send_worker(WorkerSendRequest {
                     our_tid: None,
                     message,
                     addr,
@@ -755,7 +812,7 @@ impl DhtState {
                         ..Default::default()
                     }),
                 };
-                self.worker_sender.send(WorkerSendRequest {
+                let _ = self.try_send_worker(WorkerSendRequest {
                     our_tid: None,
                     message,
                     addr,
@@ -779,7 +836,7 @@ impl DhtState {
                         )),
                     }),
                 };
-                self.worker_sender.send(WorkerSendRequest {
+                let _ = self.try_send_worker(WorkerSendRequest {
                     our_tid: None,
                     message,
                     addr,
@@ -799,7 +856,7 @@ impl DhtState {
                         ..Default::default()
                     }),
                 };
-                self.worker_sender.send(WorkerSendRequest {
+                let _ = self.try_send_worker(WorkerSendRequest {
                     our_tid: None,
                     message,
                     addr,
@@ -895,11 +952,9 @@ impl DhtWorker {
     }
 
     async fn bootstrap(&self, bootstrap_addrs: &[String]) -> anyhow::Result<()> {
-        let mut futs = FuturesUnordered::new();
-
-        for addr in bootstrap_addrs.iter() {
-            futs.push(self.bootstrap_hostname_with_backoff(addr));
-        }
+        let mut futs = futures::stream::iter(bootstrap_addrs.iter().cloned())
+            .map(|addr| async move { self.bootstrap_hostname_with_backoff(&addr).await })
+            .buffer_unordered(MAX_CONCURRENT_BOOTSTRAPS);
         let mut successes = 0;
         while let Some(resp) = futs.next().await {
             if resp.is_ok() {
@@ -913,7 +968,7 @@ impl DhtWorker {
     }
 
     async fn bucket_refresher(&self) -> anyhow::Result<()> {
-        let (tx, mut rx) = unbounded_channel();
+        let (tx, mut rx) = maintenance_channel();
 
         let mut futs = FuturesUnordered::new();
         let filler = async {
@@ -927,9 +982,11 @@ impl DhtWorker {
                     if bucket.leaf.last_refreshed.elapsed() < INACTIVITY_TIMEOUT {
                         continue;
                     }
-                    found += 1;
                     let random_id = bucket.random_within();
-                    tx.send(random_id).unwrap();
+                    if tx.try_send(random_id).is_err() {
+                        break;
+                    }
+                    found += 1;
                 }
                 trace!("iteration {}, refreshing {} buckets", iteration, found);
                 iteration += 1;
@@ -941,7 +998,7 @@ impl DhtWorker {
         loop {
             tokio::select! {
                 _ = &mut filler => {},
-                random_id = rx.recv() => {
+                random_id = rx.recv(), if can_schedule_maintenance_request(futs.len()) => {
                     let random_id = random_id.unwrap();
                     let addrs = self
                         .dht
@@ -965,7 +1022,7 @@ impl DhtWorker {
     async fn pinger(&self) -> anyhow::Result<()> {
         let mut futs = FuturesUnordered::new();
         let mut interval = tokio::time::interval(INACTIVITY_TIMEOUT / 4);
-        let (tx, mut rx) = unbounded_channel();
+        let (tx, mut rx) = maintenance_channel();
         let looper = async {
             let mut iteration = 0;
             loop {
@@ -977,8 +1034,10 @@ impl DhtWorker {
                         node.status(now),
                         NodeStatus::Questionable | NodeStatus::Unknown
                     ) {
+                        if tx.try_send((node.id(), node.addr())).is_err() {
+                            break;
+                        }
                         found += 1;
-                        tx.send((node.id(), node.addr())).unwrap();
                     }
                 }
                 trace!("iteration {}, pinging {} nodes", iteration, found);
@@ -991,7 +1050,7 @@ impl DhtWorker {
         loop {
             tokio::select! {
                 _ = &mut looper => {},
-                r = rx.recv() => {
+                r = rx.recv(), if can_schedule_maintenance_request(futs.len()) => {
                     let (id, addr) = r.unwrap();
                     futs.push(async move {
                         self.dht.routing_table.write().mark_outgoing_request(&id);
@@ -1014,7 +1073,7 @@ impl DhtWorker {
     async fn framer(
         &self,
         socket: &UdpSocket,
-        mut input_rx: UnboundedReceiver<WorkerSendRequest>,
+        mut input_rx: Receiver<WorkerSendRequest>,
         output_tx: Sender<(Message<ByteBufOwned>, SocketAddr)>,
     ) -> anyhow::Result<()> {
         let writer = async {
@@ -1076,7 +1135,7 @@ impl DhtWorker {
 
     async fn start(
         self,
-        in_rx: UnboundedReceiver<WorkerSendRequest>,
+        in_rx: Receiver<WorkerSendRequest>,
         bootstrap_addrs: &[String],
     ) -> anyhow::Result<()> {
         let (out_tx, mut out_rx) = channel(1);
@@ -1179,7 +1238,7 @@ impl DhtState {
 
             let token = config.cancellation_token.take().unwrap_or_default();
 
-            let (in_tx, in_rx) = unbounded_channel();
+            let (in_tx, in_rx) = worker_send_channel();
             let state = Arc::new(Self::new_internal(
                 peer_id,
                 in_tx,
@@ -1225,3 +1284,86 @@ impl DhtState {
         self.routing_table.read().clone()
     }
 }
+
+#[cfg(test)]
+mod queue_budget_tests {
+    use super::*;
+    use futures::FutureExt;
+
+    fn worker_request() -> WorkerSendRequest {
+        WorkerSendRequest {
+            our_tid: None,
+            message: Message {
+                transaction_id: ByteBufOwned::from(&[0, 1][..]),
+                version: None,
+                ip: None,
+                kind: MessageKind::PingRequest(PingRequest {
+                    id: Id20::new([0; 20]),
+                }),
+            },
+            addr: SocketAddr::from(([127, 0, 0, 1], 6881)),
+        }
+    }
+
+    #[tokio::test]
+    async fn queue_budgets_close_at_exact_boundaries() {
+        let (worker_tx, mut worker_rx) = worker_send_channel();
+        for _ in 0..MAX_PENDING_DHT_SENDS {
+            worker_tx.try_send(worker_request()).unwrap();
+        }
+        assert!(matches!(
+            worker_tx.try_send(worker_request()),
+            Err(TrySendError::Full(_))
+        ));
+        assert!(worker_rx.recv().await.is_some());
+        worker_tx.try_send(worker_request()).unwrap();
+
+        let addr = SocketAddr::from(([127, 0, 0, 1], 6881));
+        let (peer_tx, mut peer_rx) = discovered_peer_channel();
+        for _ in 0..MAX_PENDING_DISCOVERED_PEERS {
+            peer_tx.try_send(addr).unwrap();
+        }
+        let blocked_peer = peer_tx.send(addr);
+        tokio::pin!(blocked_peer);
+        assert!(blocked_peer.as_mut().now_or_never().is_none());
+        assert_eq!(peer_rx.recv().await, Some(addr));
+        assert!(blocked_peer.await.is_ok());
+
+        let (node_tx, mut node_rx) = recursive_node_channel();
+        for _ in 0..MAX_PENDING_RECURSIVE_NODES {
+            node_tx.try_send((None, addr, 0)).unwrap();
+        }
+        assert!(matches!(
+            node_tx.try_send((None, addr, 0)),
+            Err(TrySendError::Full(_))
+        ));
+        assert!(node_rx.recv().await.is_some());
+        node_tx.try_send((None, addr, 0)).unwrap();
+
+        assert!(can_schedule_recursive_request(
+            MAX_CONCURRENT_RECURSIVE_REQUESTS - 1
+        ));
+        assert!(!can_schedule_recursive_request(
+            MAX_CONCURRENT_RECURSIVE_REQUESTS
+        ));
+
+        let (maintenance_tx, mut maintenance_rx) = maintenance_channel();
+        for _ in 0..MAX_PENDING_MAINTENANCE_REQUESTS {
+            maintenance_tx.try_send(Id20::new([0; 20])).unwrap();
+        }
+        assert!(matches!(
+            maintenance_tx.try_send(Id20::new([0; 20])),
+            Err(TrySendError::Full(_))
+        ));
+        assert!(maintenance_rx.recv().await.is_some());
+        maintenance_tx.try_send(Id20::new([0; 20])).unwrap();
+
+        assert!(can_schedule_maintenance_request(
+            MAX_CONCURRENT_MAINTENANCE_REQUESTS - 1
+        ));
+        assert!(!can_schedule_maintenance_request(
+            MAX_CONCURRENT_MAINTENANCE_REQUESTS
+        ));
+        assert_eq!(MAX_CONCURRENT_BOOTSTRAPS, 8);
+    }
+}
diff --git a/crates/librqbit/src/dht_utils.rs b/crates/librqbit/src/dht_utils.rs
index 6e200b12..4455bf44 100644
--- a/crates/librqbit/src/dht_utils.rs
+++ b/crates/librqbit/src/dht_utils.rs
@@ -12,6 +12,31 @@ use crate::{
 };
 use librqbit_core::hash_id::Id20;
 
+const MAX_CONCURRENT_METADATA_PEERS: usize = 128;
+const MAX_METADATA_PEER_CANDIDATES: usize = 4096;
+
+#[derive(Debug, PartialEq, Eq)]
+enum CandidateAdmission {
+    Admitted,
+    Duplicate,
+    LimitReached,
+}
+
+fn can_schedule_metadata_peer(active: usize) -> bool {
+    active < MAX_CONCURRENT_METADATA_PEERS
+}
+
+fn admit_metadata_peer(seen: &mut HashSet<SocketAddr>, addr: SocketAddr) -> CandidateAdmission {
+    if seen.contains(&addr) {
+        return CandidateAdmission::Duplicate;
+    }
+    if seen.len() >= MAX_METADATA_PEER_CANDIDATES {
+        return CandidateAdmission::LimitReached;
+    }
+    seen.insert(addr);
+    CandidateAdmission::Admitted
+}
+
 #[derive(Debug)]
 #[allow(clippy::large_enum_variant)]
 pub enum ReadMetainfoResult<Rx> {
@@ -37,14 +62,13 @@ pub async fn read_metainfo_from_peer_receiver<A: Stream<Item = SocketAddr> + Unp
 ) -> ReadMetainfoResult<A> {
     let mut seen = HashSet::<SocketAddr>::new();
     let mut addrs = addrs_stream;
-
-    let semaphore = tokio::sync::Semaphore::new(128);
+    let mut initial_addrs = initial_addrs.into_iter();
+    let mut initial_addrs_completed = false;
+    let mut candidate_limit_reached = false;
 
     let read_info_guarded = |addr| {
-        let semaphore = &semaphore;
         let connector = connector.clone();
         async move {
-            let token = semaphore.acquire().await?;
             let ret = peer_info_reader::read_metainfo_from_peer(
                 addr,
                 peer_id,
@@ -56,22 +80,38 @@ pub async fn read_metainfo_from_peer_receiver<A: Stream<Item = SocketAddr> + Unp
             .instrument(error_span!("read_metainfo_from_peer", ?addr))
             .await
             .with_context(|| format!("error reading metainfo from {addr}"));
-            drop(token);
             ret
         }
     };
 
     let mut unordered = FuturesUnordered::new();
 
-    for a in initial_addrs {
-        seen.insert(a);
-        unordered.push(read_info_guarded(a));
-    }
-
     let mut addrs_completed = false;
 
     loop {
-        if addrs_completed && unordered.is_empty() {
+        while !initial_addrs_completed && can_schedule_metadata_peer(unordered.len()) {
+            let Some(addr) = initial_addrs.next() else {
+                initial_addrs_completed = true;
+                break;
+            };
+            match admit_metadata_peer(&mut seen, addr) {
+                CandidateAdmission::Admitted => unordered.push(read_info_guarded(addr)),
+                CandidateAdmission::Duplicate => {}
+                CandidateAdmission::LimitReached => {
+                    candidate_limit_reached = true;
+                    initial_addrs_completed = true;
+                }
+            }
+        }
+
+        if candidate_limit_reached && unordered.is_empty() {
+            debug!(
+                limit = MAX_METADATA_PEER_CANDIDATES,
+                "metadata peer candidate limit reached"
+            );
+            return ReadMetainfoResult::ChannelClosed { seen };
+        }
+        if initial_addrs_completed && addrs_completed && unordered.is_empty() {
             return ReadMetainfoResult::ChannelClosed { seen };
         }
 
@@ -86,11 +126,16 @@ pub async fn read_metainfo_from_peer_receiver<A: Stream<Item = SocketAddr> + Unp
                 }
             }
 
-            next_addr = addrs.next(), if !addrs_completed => {
+            next_addr = addrs.next(), if initial_addrs_completed
+                && !addrs_completed
+                && !candidate_limit_reached
+                && can_schedule_metadata_peer(unordered.len()) => {
                 match next_addr {
                     Some(addr) => {
-                        if seen.insert(addr) {
-                            unordered.push(read_info_guarded(addr));
+                        match admit_metadata_peer(&mut seen, addr) {
+                            CandidateAdmission::Admitted => unordered.push(read_info_guarded(addr)),
+                            CandidateAdmission::Duplicate => {}
+                            CandidateAdmission::LimitReached => candidate_limit_reached = true,
                         }
                         continue;
                     },
@@ -116,6 +161,34 @@ mod tests {
 
     static LOG_INIT: Once = Once::new();
 
+    #[test]
+    fn metadata_peer_budgets_close_at_exact_boundaries() {
+        assert!(can_schedule_metadata_peer(
+            MAX_CONCURRENT_METADATA_PEERS - 1
+        ));
+        assert!(!can_schedule_metadata_peer(MAX_CONCURRENT_METADATA_PEERS));
+
+        let mut seen = HashSet::new();
+        for port in 1..=MAX_METADATA_PEER_CANDIDATES {
+            let addr = SocketAddr::from(([127, 0, 0, 1], port as u16));
+            assert_eq!(
+                admit_metadata_peer(&mut seen, addr),
+                CandidateAdmission::Admitted
+            );
+        }
+        let duplicate = SocketAddr::from(([127, 0, 0, 1], 1));
+        assert_eq!(
+            admit_metadata_peer(&mut seen, duplicate),
+            CandidateAdmission::Duplicate
+        );
+        let overflow = SocketAddr::from(([127, 0, 0, 2], 1));
+        assert_eq!(
+            admit_metadata_peer(&mut seen, overflow),
+            CandidateAdmission::LimitReached
+        );
+        assert_eq!(seen.len(), MAX_METADATA_PEER_CANDIDATES);
+    }
+
     fn init_logging() {
         #[allow(unused_must_use)]
         LOG_INIT.call_once(|| {
