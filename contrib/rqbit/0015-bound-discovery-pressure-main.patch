diff --git a/crates/dht/src/dht.rs b/crates/dht/src/dht.rs
index c2defd32..da7dc931 100644
--- a/crates/dht/src/dht.rs
+++ b/crates/dht/src/dht.rs
@@ -38,7 +38,7 @@ use librqbit_dualstack_sockets::{BindDevice, UdpSocket};
 use parking_lot::RwLock;
 
 use serde::Serialize;
-use tokio::sync::mpsc::{Sender, UnboundedReceiver, UnboundedSender, channel, unbounded_channel};
+use tokio::sync::mpsc::{Receiver, Sender, channel, error::TrySendError};
 
 use tokio_util::sync::CancellationToken;
 use tracing::{Instrument, debug, debug_span, error, info, trace, warn};
@@ -67,6 +67,83 @@ pub struct WorkerSendRequest {
     addr: SocketAddr,
 }
 
+const MAX_PENDING_DHT_SENDS: usize = 256;
+const MAX_PENDING_DISCOVERED_PEERS: usize = 256;
+const MAX_PENDING_RECURSIVE_NODES: usize = 256;
+const MAX_CONCURRENT_RECURSIVE_REQUESTS: usize = 32;
+const MAX_PENDING_MAINTENANCE_REQUESTS: usize = 256;
+const MAX_CONCURRENT_MAINTENANCE_REQUESTS: usize = 32;
+
+type RecursiveNode = (Option<Id20>, SocketAddr, usize);
+
+fn worker_send_channel() -> (Sender<WorkerSendRequest>, Receiver<WorkerSendRequest>) {
+    channel(MAX_PENDING_DHT_SENDS)
+}
+
+fn discovered_peer_channel() -> (Sender<SocketAddr>, Receiver<SocketAddr>) {
+    channel(MAX_PENDING_DISCOVERED_PEERS)
+}
+
+fn recursive_node_channel() -> (Sender<RecursiveNode>, Receiver<RecursiveNode>) {
+    channel(MAX_PENDING_RECURSIVE_NODES)
+}
+
+fn maintenance_channel<T>() -> (Sender<T>, Receiver<T>) {
+    channel(MAX_PENDING_MAINTENANCE_REQUESTS)
+}
+
+struct BoundedFuturesUnordered<F> {
+    futures: FuturesUnordered<F>,
+    limit: usize,
+}
+
+impl<F> BoundedFuturesUnordered<F> {
+    fn new(limit: usize) -> Self {
+        Self {
+            futures: FuturesUnordered::new(),
+            limit,
+        }
+    }
+
+    fn try_push(&self, future: F) -> Result<(), F> {
+        if self.futures.len() >= self.limit {
+            return Err(future);
+        }
+        self.futures.push(future);
+        Ok(())
+    }
+
+    fn is_empty(&self) -> bool {
+        self.futures.is_empty()
+    }
+
+    fn is_full(&self) -> bool {
+        self.futures.len() >= self.limit
+    }
+
+    async fn next(&mut self) -> Option<F::Output>
+    where
+        F: std::future::Future,
+    {
+        self.futures.next().await
+    }
+}
+
+#[derive(Debug, PartialEq, Eq)]
+enum RootQueueStatus {
+    Empty,
+    Queued(usize),
+    Saturated,
+}
+
+fn root_queue_delay(status: &RootQueueStatus) -> Duration {
+    match status {
+        RootQueueStatus::Empty => Duration::from_secs(1),
+        RootQueueStatus::Queued(n) if *n < 8 => REQUERY_INTERVAL / 8 * (*n as u32),
+        RootQueueStatus::Queued(_) | RootQueueStatus::Saturated => REQUERY_INTERVAL,
+    }
+}
+
 #[derive(Debug)]
 struct MaybeUsefulNode {
     id: Id20,
@@ -149,11 +226,13 @@ impl RecursiveRequestCallbacks for RecursiveRequestCallbacksGetPeers {
             addr,
         );
 
-        let _ = req.dht.worker_sender.send(WorkerSendRequest {
+        if let Err(error) = req.dht.try_send_worker(WorkerSendRequest {
             our_tid: Some(tid),
             message,
             addr,
-        });
+        }) {
+            debug!(?error, "failed to enqueue DHT announce");
+        }
     }
 }
 
@@ -192,22 +271,22 @@ struct RecursiveRequest<C: RecursiveRequestCallbacks> {
     request: Request,
     dht: Arc<DhtState>,
     useful_nodes: RwLock<Vec<MaybeUsefulNode>>,
-    peer_tx: tokio::sync::mpsc::UnboundedSender<SocketAddr>,
-    node_tx: tokio::sync::mpsc::UnboundedSender<(Option<Id20>, SocketAddr, usize)>,
+    peer_tx: Sender<SocketAddr>,
+    node_tx: Sender<RecursiveNode>,
     callbacks: C,
 }
 
 pub struct RequestPeersStream {
-    rx: tokio::sync::mpsc::UnboundedReceiver<SocketAddr>,
+    rx: Receiver<SocketAddr>,
     cancel_join_handle_v4: tokio::task::JoinHandle<()>,
     cancel_join_handle_v6: tokio::task::JoinHandle<()>,
 }
 
 impl RequestPeersStream {
     fn new(dht: Arc<DhtState>, info_hash: Id20, announce_port: Option<u16>) -> Self {
-        let (peer_tx, peer_rx) = unbounded_channel();
-        let make = |is_v4: bool, dht: Arc<DhtState>, peer_tx: UnboundedSender<SocketAddr>| {
-            let (node_tx, node_rx) = unbounded_channel();
+        let (peer_tx, peer_rx) = discovered_peer_channel();
+        let make = |is_v4: bool, dht: Arc<DhtState>, peer_tx: Sender<SocketAddr>| {
+            let (node_tx, node_rx) = recursive_node_channel();
             let rp = Arc::new(RecursiveRequest {
                 max_depth: 4,
                 info_hash,
@@ -264,7 +343,7 @@ impl RecursiveRequest<RecursiveRequestCallbacksFindNodes> {
         target: Id20,
         addrs: impl Iterator<Item = SocketAddr>,
     ) -> crate::Result<()> {
-        let (node_tx, mut node_rx) = unbounded_channel();
+        let (node_tx, mut node_rx) = recursive_node_channel();
         let req = RecursiveRequest {
             max_depth: 4,
             info_hash: target,
@@ -272,7 +351,7 @@ impl RecursiveRequest<RecursiveRequestCallbacksFindNodes> {
             dht,
             useful_nodes_limit: 32,
             useful_nodes: RwLock::new(Vec::new()),
-            peer_tx: unbounded_channel().0,
+            peer_tx: discovered_peer_channel().0,
             node_tx,
             callbacks: RecursiveRequestCallbacksFindNodes {},
         };
@@ -290,33 +369,33 @@ impl RecursiveRequest<RecursiveRequestCallbacksFindNodes> {
                 ))
         };
 
-        let mut futs = FuturesUnordered::new();
-
         let mut initial_addrs = 0;
         for addr in addrs {
-            futs.push(request_one(None, addr, 0));
-            initial_addrs += 1;
+            match req.node_tx.try_send((None, addr, 0)) {
+                Ok(()) => initial_addrs += 1,
+                Err(TrySendError::Full(_)) => break,
+                Err(TrySendError::Closed(_)) => return Err(Error::DhtDead),
+            }
         }
 
+        let mut futs = BoundedFuturesUnordered::new(MAX_CONCURRENT_RECURSIVE_REQUESTS);
         let mut successes = 0;
         let mut errors = 0;
 
         loop {
+            if futs.is_empty() && node_rx.is_empty() {
+                break;
+            }
             tokio::select! {
                 biased;
 
-                r = node_rx.recv() => {
-                    let (id, addr, depth) = r.unwrap();
-                    futs.push(request_one(id, addr, depth))
-                },
-                f = futs.next() => {
-                    let f = match f {
-                        Some(f) => f,
-                        None => {
-                            // find_node recursion finished.
-                            break;
-                        }
+                r = node_rx.recv(), if !futs.is_full() => {
+                    let Some((id, addr, depth)) = r else {
+                        break;
                     };
+                    debug_assert!(futs.try_push(request_one(id, addr, depth)).is_ok());
+                },
+                Some(f) = futs.next(), if !futs.is_empty() => {
                     if f.is_ok() {
                         successes += 1;
                     } else {
@@ -338,7 +417,7 @@ impl RecursiveRequest<RecursiveRequestCallbacksFindNodes> {
 impl RecursiveRequest<RecursiveRequestCallbacksGetPeers> {
     fn request_peers_forever(
         self: &Arc<Self>,
-        mut node_rx: tokio::sync::mpsc::UnboundedReceiver<(Option<Id20>, SocketAddr, usize)>,
+        mut node_rx: Receiver<RecursiveNode>,
         is_v4: bool,
     ) -> tokio::task::JoinHandle<()> {
         let this = self.clone();
@@ -354,9 +433,7 @@ impl RecursiveRequest<RecursiveRequestCallbacksGetPeers> {
                         loop {
                             trace!("iteration {}", iteration);
                             let sleep = match this.get_peers_root(is_v4) {
-                                Ok(0) => Duration::from_secs(1),
-                                Ok(n) if n < 8 => REQUERY_INTERVAL / 8 * (n as u32),
-                                Ok(_) => REQUERY_INTERVAL,
+                                Ok(status) => root_queue_delay(&status),
                                 Err(e) => {
                                     error!("dht: error in get_peers_root(): {e:#}");
                                     return Err::<(), crate::Error>(e);
@@ -369,16 +446,16 @@ impl RecursiveRequest<RecursiveRequestCallbacksGetPeers> {
                 };
                 tokio::pin!(looper);
 
-                let mut futs = FuturesUnordered::new();
+                let mut futs = BoundedFuturesUnordered::new(MAX_CONCURRENT_RECURSIVE_REQUESTS);
                 loop {
                     tokio::select! {
-                        addr = node_rx.recv() => {
+                        addr = node_rx.recv(), if !futs.is_full() => {
                             let (id, addr, depth) = addr.unwrap();
-                            futs.push(
+                            debug_assert!(futs.try_push(
                                 this.request_one(id, addr, depth)
                                     .map_err(|e| debug!("error: {e:#}"))
                                     .instrument(debug_span!("addr", addr=addr.to_string()))
-                            );
+                            ).is_ok());
                         }
                         Some(_) = futs.next(), if !futs.is_empty() => {}
                         r = &mut looper => {
@@ -390,7 +467,7 @@ impl RecursiveRequest<RecursiveRequestCallbacksGetPeers> {
         )
     }
 
-    fn get_peers_root(&self, is_v4: bool) -> crate::Result<usize> {
+    fn get_peers_root(&self, is_v4: bool) -> crate::Result<RootQueueStatus> {
         let mut count = 0;
         let table = if is_v4 {
             &self.dht.routing_table_v4
@@ -404,14 +481,18 @@ impl RecursiveRequest<RecursiveRequestCallbacksGetPeers> {
             .map(|n| (n.id(), n.addr()))
             .take(8)
         {
-            count += 1;
-            self.node_tx
-                .send((Some(id), addr, 0))
-                .ok()
-                .ok_or(Error::DhtDead)?;
+            match self.node_tx.try_send((Some(id), addr, 0)) {
+                Ok(()) => count += 1,
+                Err(TrySendError::Full(_)) => return Ok(RootQueueStatus::Saturated),
+                Err(TrySendError::Closed(_)) => return Err(Error::DhtDead),
+            }
         }
 
-        Ok(count)
+        if count == 0 {
+            Ok(RootQueueStatus::Empty)
+        } else {
+            Ok(RootQueueStatus::Queued(count))
+        }
     }
 }
 
@@ -426,6 +507,7 @@ impl<C: RecursiveRequestCallbacks> RecursiveRequest<C> {
             self.callbacks.on_request_start(self, id, addr);
         }
 
+        // Send once: callbacks and traversal must consume the same response.
         let response = self
             .dht
             .request(self.request.clone(), addr)
@@ -437,7 +519,7 @@ impl<C: RecursiveRequestCallbacks> RecursiveRequest<C> {
             self.callbacks.on_request_end(self, id, addr, &response);
         }
 
-        let response = match self.dht.request(self.request.clone(), addr).await {
+        let response = match response {
             Ok(ResponseOrError::Response(r)) => r,
             Ok(ResponseOrError::Error(e)) => {
                 debug!("error response: {e:?}");
@@ -449,12 +531,6 @@ impl<C: RecursiveRequestCallbacks> RecursiveRequest<C> {
             }
         };
 
-        if let Some(peers) = response.values {
-            for peer in peers {
-                self.peer_tx.send(peer.0).ok().ok_or(Error::ReceiverDead)?;
-            }
-        }
-
         let node_it = response
             .nodes
             .iter()
@@ -476,10 +552,25 @@ impl<C: RecursiveRequestCallbacks> RecursiveRequest<C> {
                 should_request, node.id, node.addr, depth, self.max_depth
             );
             if should_request {
-                self.node_tx
-                    .send((Some(node.id), node.addr, depth + 1))
-                    .ok()
-                    .ok_or(Error::ReceiverDead)?;
+                match self.node_tx.try_send((Some(node.id), node.addr, depth + 1)) {
+                    Ok(()) => {}
+                    Err(TrySendError::Full(_)) => {
+                        trace!(addr = %node.addr, "recursive request queue is full, dropping node")
+                    }
+                    Err(TrySendError::Closed(_)) => return Err(Error::ReceiverDead),
+                }
+            }
+        }
+        if let Some(peers) = response.values {
+            for peer in peers {
+                let addr = peer.0;
+                match self.peer_tx.try_send(addr) {
+                    Ok(()) => {}
+                    Err(TrySendError::Full(_)) => {
+                        trace!(%addr, "discovered peer queue is full, dropping peer")
+                    }
+                    Err(TrySendError::Closed(_)) => return Err(Error::ReceiverDead),
+                }
             }
         }
         Ok(())
@@ -585,7 +676,7 @@ pub struct DhtState {
     // Sending requests to the worker.
     rate_limiter: RateLimiter,
     // This is to send raw messages
-    worker_sender: UnboundedSender<WorkerSendRequest>,
+    worker_sender: Sender<WorkerSendRequest>,
 
     cancellation_token: CancellationToken,
 
@@ -595,7 +686,7 @@ pub struct DhtState {
 impl DhtState {
     fn new_internal(
         id: Id20,
-        sender: UnboundedSender<WorkerSendRequest>,
+        sender: Sender<WorkerSendRequest>,
         routing_table_v4: Option<RoutingTable>,
         routing_table_v6: Option<RoutingTable>,
         listen_addr: SocketAddr,
@@ -618,25 +709,35 @@ impl DhtState {
         }
     }
 
+    fn try_send_worker(&self, request: WorkerSendRequest) -> crate::Result<()> {
+        match self.worker_sender.try_send(request) {
+            Ok(()) => Ok(()),
+            Err(TrySendError::Full(_)) => {
+                debug!("DHT send queue is full, dropping untracked datagram");
+                Ok(())
+            }
+            Err(TrySendError::Closed(_)) => Err(Error::DhtDead),
+        }
+    }
+
     async fn request(&self, request: Request, addr: SocketAddr) -> crate::Result<ResponseOrError> {
         self.rate_limiter.acquire_one().await;
+        let permit = self
+            .worker_sender
+            .reserve()
+            .await
+            .map_err(|_| Error::DhtDead)?;
         let (tid, message) = self.create_request(request, addr);
         let key = (tid, addr);
         let (tx, rx) = tokio::sync::oneshot::channel();
         self.inflight_by_transaction_id
             .insert(key, OutstandingRequest { done: tx });
         trace!("sending {message:?}");
-        match self.worker_sender.send(WorkerSendRequest {
+        permit.send(WorkerSendRequest {
             our_tid: Some(tid),
             message,
             addr,
-        }) {
-            Ok(_) => {}
-            Err(_) => {
-                self.inflight_by_transaction_id.remove(&key);
-                return Err(Error::DhtDead);
-            }
-        };
+        });
         match tokio::time::timeout(RESPONSE_TIMEOUT, rx).await {
             Ok(Ok(r)) => r.map(|r| {
                 trace!("received {r:?}");
@@ -827,14 +928,11 @@ impl DhtState {
                 self.get_table_for_addr(addr)
                     .write()
                     .mark_last_query(&req.id, now());
-                self.worker_sender
-                    .send(WorkerSendRequest {
-                        our_tid: None,
-                        message,
-                        addr,
-                    })
-                    .ok()
-                    .ok_or(Error::DhtDead)?;
+                self.try_send_worker(WorkerSendRequest {
+                    our_tid: None,
+                    message,
+                    addr,
+                })?;
                 Ok(())
             }
             MessageKind::AnnouncePeer(ann) => {
@@ -852,14 +950,11 @@ impl DhtState {
                         ..Default::default()
                     }),
                 };
-                self.worker_sender
-                    .send(WorkerSendRequest {
-                        our_tid: None,
-                        message,
-                        addr,
-                    })
-                    .ok()
-                    .ok_or(Error::DhtDead)?;
+                self.try_send_worker(WorkerSendRequest {
+                    our_tid: None,
+                    message,
+                    addr,
+                })?;
                 Ok(())
             }
             MessageKind::GetPeersRequest(req) => {
@@ -885,14 +980,11 @@ impl DhtState {
                         )),
                     }),
                 };
-                self.worker_sender
-                    .send(WorkerSendRequest {
-                        our_tid: None,
-                        message,
-                        addr,
-                    })
-                    .ok()
-                    .ok_or(Error::DhtDead)?;
+                self.try_send_worker(WorkerSendRequest {
+                    our_tid: None,
+                    message,
+                    addr,
+                })?;
                 Ok(())
             }
             MessageKind::FindNodeRequest(req) => {
@@ -914,14 +1006,11 @@ impl DhtState {
                         ..Default::default()
                     }),
                 };
-                self.worker_sender
-                    .send(WorkerSendRequest {
-                        our_tid: None,
-                        message,
-                        addr,
-                    })
-                    .ok()
-                    .ok_or(Error::DhtDead)?;
+                self.try_send_worker(WorkerSendRequest {
+                    our_tid: None,
+                    message,
+                    addr,
+                })?;
                 Ok(())
             }
             _ => unreachable!(),
@@ -1019,8 +1108,7 @@ impl DhtWorker {
 
     async fn bootstrap(&self, bootstrap_addrs: &[String]) -> crate::Result<()> {
         let mut futs = FuturesUnordered::new();
-
-        for addr in bootstrap_addrs.iter() {
+        for addr in bootstrap_addrs {
             futs.push(self.bootstrap_hostname_with_backoff(addr));
         }
         let mut successes = 0;
@@ -1036,7 +1124,7 @@ impl DhtWorker {
     }
 
     async fn bucket_refresher(&self, is_v4: bool) -> crate::Result<()> {
-        let (tx, mut rx) = unbounded_channel();
+        let (tx, mut rx) = maintenance_channel();
 
         let table = if is_v4 {
             &self.dht.routing_table_v4
@@ -1044,7 +1132,7 @@ impl DhtWorker {
             &self.dht.routing_table_v6
         };
 
-        let mut futs = FuturesUnordered::new();
+        let mut futs = BoundedFuturesUnordered::new(MAX_CONCURRENT_MAINTENANCE_REQUESTS);
         let filler = async {
             let mut interval = tokio::time::interval(INACTIVITY_TIMEOUT);
             interval.tick().await;
@@ -1058,9 +1146,11 @@ impl DhtWorker {
                     if now - bucket.leaf.last_refreshed < INACTIVITY_TIMEOUT {
                         continue;
                     }
-                    found += 1;
                     let random_id = bucket.random_within();
-                    tx.send(random_id).unwrap();
+                    if tx.try_send(random_id).is_err() {
+                        break;
+                    }
+                    found += 1;
                 }
                 trace!("iteration {}, refreshing {} buckets", iteration, found);
                 iteration += 1;
@@ -1072,7 +1162,7 @@ impl DhtWorker {
         loop {
             tokio::select! {
                 _ = &mut filler => {},
-                random_id = rx.recv() => {
+                random_id = rx.recv(), if !futs.is_full() => {
                     let random_id = random_id.unwrap();
                     let addrs = table
                         .read()
@@ -1080,11 +1170,11 @@ impl DhtWorker {
                         .iter()
                         .map(|n| n.addr())
                         .take(8).collect::<Vec<_>>();
-                    futs.push(
+                    debug_assert!(futs.try_push(
                         RecursiveRequest::find_node_for_routing_table(
                             self.dht.clone(), random_id, addrs.into_iter()
                         ).instrument(debug_span!("refresh_bucket"))
-                    );
+                    ).is_ok());
                 },
                 _ = futs.next(), if !futs.is_empty() => {},
             }
@@ -1097,9 +1187,9 @@ impl DhtWorker {
         } else {
             &self.dht.routing_table_v6
         };
-        let mut futs = FuturesUnordered::new();
+        let mut futs = BoundedFuturesUnordered::new(MAX_CONCURRENT_MAINTENANCE_REQUESTS);
         let mut interval = tokio::time::interval(INACTIVITY_TIMEOUT / 4);
-        let (tx, mut rx) = unbounded_channel();
+        let (tx, mut rx) = maintenance_channel();
         let looper = async {
             let mut iteration = 0;
             loop {
@@ -1111,8 +1201,10 @@ impl DhtWorker {
                         node.status(now),
                         NodeStatus::Questionable | NodeStatus::Unknown
                     ) {
+                        if tx.try_send((node.id(), node.addr())).is_err() {
+                            break;
+                        }
                         found += 1;
-                        tx.send((node.id(), node.addr())).unwrap();
                     }
                 }
                 trace!("iteration {}, pinging {} nodes", iteration, found);
@@ -1125,9 +1217,9 @@ impl DhtWorker {
         loop {
             tokio::select! {
                 _ = &mut looper => {},
-                r = rx.recv() => {
+                r = rx.recv(), if !futs.is_full() => {
                     let (id, addr) = r.unwrap();
-                    futs.push(async move {
+                    debug_assert!(futs.try_push(async move {
                         table.write().mark_outgoing_request(&id, now());
                         match self.dht.request(Request::Ping, addr).await {
                             Ok(_) => {
@@ -1138,7 +1230,7 @@ impl DhtWorker {
                                 debug!("error: {e:#}");
                             }
                         }
-                    }.instrument(debug_span!("ping", addr=addr.to_string())))
+                    }.instrument(debug_span!("ping", addr=addr.to_string()))).is_ok());
                 },
                 _ = futs.next(), if !futs.is_empty() => {},
             }
@@ -1148,7 +1240,7 @@ impl DhtWorker {
     async fn framer(
         &self,
         socket: &UdpSocket,
-        mut input_rx: UnboundedReceiver<WorkerSendRequest>,
+        mut input_rx: Receiver<WorkerSendRequest>,
         output_tx: Sender<(Message<ByteBufOwned>, SocketAddr)>,
     ) -> crate::Result<()> {
         let writer = async {
@@ -1202,7 +1294,7 @@ impl DhtWorker {
 
     async fn start(
         self,
-        in_rx: UnboundedReceiver<WorkerSendRequest>,
+        in_rx: Receiver<WorkerSendRequest>,
         bootstrap_addrs: &[String],
     ) -> crate::Result<()> {
         let (out_tx, mut out_rx) = channel(1);
@@ -1322,7 +1414,7 @@ impl DhtState {
 
             let token = config.cancellation_token.take().unwrap_or_default();
 
-            let (in_tx, in_rx) = unbounded_channel();
+            let (in_tx, in_rx) = worker_send_channel();
             let state = Arc::new(Self::new_internal(
                 peer_id,
                 in_tx,
@@ -1375,6 +1467,88 @@ impl DhtState {
     // }
 }
 
+#[cfg(test)]
+mod queue_budget_tests {
+    use super::*;
+
+    fn worker_request() -> WorkerSendRequest {
+        WorkerSendRequest {
+            our_tid: None,
+            message: Message {
+                transaction_id: ByteBufOwned::from(&[0, 1][..]),
+                version: None,
+                ip: None,
+                kind: MessageKind::PingRequest(PingRequest {
+                    id: Id20::new([0; 20]),
+                }),
+            },
+            addr: SocketAddr::from(([127, 0, 0, 1], 6881)),
+        }
+    }
+
+    #[tokio::test]
+    async fn queue_budgets_close_at_exact_boundaries() {
+        let (worker_tx, mut worker_rx) = worker_send_channel();
+        for _ in 0..MAX_PENDING_DHT_SENDS {
+            worker_tx.try_send(worker_request()).unwrap();
+        }
+        assert!(matches!(
+            worker_tx.try_send(worker_request()),
+            Err(TrySendError::Full(_))
+        ));
+        assert!(worker_rx.recv().await.is_some());
+        worker_tx.try_send(worker_request()).unwrap();
+
+        let addr = SocketAddr::from(([127, 0, 0, 1], 6881));
+        let (peer_tx, mut peer_rx) = discovered_peer_channel();
+        for _ in 0..MAX_PENDING_DISCOVERED_PEERS {
+            peer_tx.try_send(addr).unwrap();
+        }
+        assert!(matches!(peer_tx.try_send(addr), Err(TrySendError::Full(_))));
+        assert_eq!(peer_rx.recv().await, Some(addr));
+        peer_tx.try_send(addr).unwrap();
+
+        let (node_tx, mut node_rx) = recursive_node_channel();
+        for _ in 0..MAX_PENDING_RECURSIVE_NODES {
+            node_tx.try_send((None, addr, 0)).unwrap();
+        }
+        assert!(matches!(
+            node_tx.try_send((None, addr, 0)),
+            Err(TrySendError::Full(_))
+        ));
+        assert!(node_rx.recv().await.is_some());
+        node_tx.try_send((None, addr, 0)).unwrap();
+
+        let recursive = BoundedFuturesUnordered::new(MAX_CONCURRENT_RECURSIVE_REQUESTS);
+        for _ in 0..MAX_CONCURRENT_RECURSIVE_REQUESTS {
+            assert!(recursive.try_push(std::future::pending::<()>()).is_ok());
+        }
+        assert!(recursive.try_push(std::future::pending::<()>()).is_err());
+
+        let (maintenance_tx, mut maintenance_rx) = maintenance_channel();
+        for _ in 0..MAX_PENDING_MAINTENANCE_REQUESTS {
+            maintenance_tx.try_send(Id20::new([0; 20])).unwrap();
+        }
+        assert!(matches!(
+            maintenance_tx.try_send(Id20::new([0; 20])),
+            Err(TrySendError::Full(_))
+        ));
+        assert!(maintenance_rx.recv().await.is_some());
+        maintenance_tx.try_send(Id20::new([0; 20])).unwrap();
+
+        let maintenance = BoundedFuturesUnordered::new(MAX_CONCURRENT_MAINTENANCE_REQUESTS);
+        for _ in 0..MAX_CONCURRENT_MAINTENANCE_REQUESTS {
+            assert!(maintenance.try_push(std::future::pending::<()>()).is_ok());
+        }
+        assert!(maintenance.try_push(std::future::pending::<()>()).is_err());
+
+        assert_eq!(
+            root_queue_delay(&RootQueueStatus::Saturated),
+            REQUERY_INTERVAL
+        );
+    }
+}
+
 trait FromSocketAddr: Sized {
     fn from_socket_addr(addr: SocketAddr) -> Option<Self>;
 }
diff --git a/crates/librqbit/src/dht_utils.rs b/crates/librqbit/src/dht_utils.rs
index f2f35d06..0fa89150 100644
--- a/crates/librqbit/src/dht_utils.rs
+++ b/crates/librqbit/src/dht_utils.rs
@@ -1,4 +1,8 @@
-use std::{collections::HashSet, net::SocketAddr, sync::Arc};
+use std::{
+    collections::{HashSet, VecDeque},
+    net::SocketAddr,
+    sync::Arc,
+};
 
 use anyhow::Context;
 use buffers::ByteBufOwned;
@@ -12,6 +16,85 @@ use crate::{
 };
 use librqbit_core::hash_id::Id20;
 
+const MAX_CONCURRENT_METADATA_PEERS: usize = 128;
+const MAX_PENDING_METADATA_PEERS: usize = 256;
+const MAX_METADATA_PEER_CANDIDATES: usize = 4096;
+
+#[derive(Debug, PartialEq, Eq)]
+enum CandidateAdmission {
+    Admitted,
+    Duplicate,
+    Untracked,
+}
+
+fn admit_metadata_peer(seen: &mut HashSet<SocketAddr>, addr: SocketAddr) -> CandidateAdmission {
+    if seen.contains(&addr) {
+        return CandidateAdmission::Duplicate;
+    }
+    if seen.len() >= MAX_METADATA_PEER_CANDIDATES {
+        return CandidateAdmission::Untracked;
+    }
+    seen.insert(addr);
+    CandidateAdmission::Admitted
+}
+
+struct MetadataPeerQueues<F> {
+    active: FuturesUnordered<F>,
+    pending: VecDeque<SocketAddr>,
+}
+
+impl<F> MetadataPeerQueues<F> {
+    fn new() -> Self {
+        Self {
+            active: FuturesUnordered::new(),
+            pending: VecDeque::new(),
+        }
+    }
+
+    fn can_accept(&self) -> bool {
+        self.active.len() < MAX_CONCURRENT_METADATA_PEERS
+            || self.pending.len() < MAX_PENDING_METADATA_PEERS
+    }
+
+    fn enqueue<M>(&mut self, addr: SocketAddr, make_future: &M) -> bool
+    where
+        M: Fn(SocketAddr) -> F,
+    {
+        if self.active.len() < MAX_CONCURRENT_METADATA_PEERS {
+            self.active.push(make_future(addr));
+            true
+        } else if self.pending.len() < MAX_PENDING_METADATA_PEERS {
+            self.pending.push_back(addr);
+            true
+        } else {
+            false
+        }
+    }
+
+    fn promote_pending<M>(&mut self, make_future: &M)
+    where
+        M: Fn(SocketAddr) -> F,
+    {
+        while self.active.len() < MAX_CONCURRENT_METADATA_PEERS {
+            let Some(addr) = self.pending.pop_front() else {
+                break;
+            };
+            self.active.push(make_future(addr));
+        }
+    }
+
+    fn is_empty(&self) -> bool {
+        self.active.is_empty() && self.pending.is_empty()
+    }
+
+    async fn next(&mut self) -> Option<F::Output>
+    where
+        F: std::future::Future,
+    {
+        self.active.next().await
+    }
+}
+
 #[derive(Debug)]
 #[allow(clippy::large_enum_variant)]
 pub enum ReadMetainfoResult<Rx> {
@@ -38,15 +121,14 @@ pub async fn read_metainfo_from_peer_receiver<A: Stream<Item = SocketAddr> + Unp
 ) -> ReadMetainfoResult<A> {
     let mut seen = HashSet::<SocketAddr>::new();
     let mut addrs = addrs_stream;
-
-    let semaphore = tokio::sync::Semaphore::new(128);
+    let mut initial_addrs = VecDeque::from(initial_addrs);
+    let mut initial_addrs_completed = false;
+    let mut candidate_limit_logged = false;
 
     let read_info_guarded = |addr| {
-        let semaphore = &semaphore;
         let connector = connector.clone();
         let client_name_and_version = client_name_and_version.clone();
         async move {
-            let token = semaphore.acquire().await?;
             let ret = peer_info_reader::read_metainfo_from_peer(
                 addr,
                 peer_id,
@@ -61,27 +143,46 @@ pub async fn read_metainfo_from_peer_receiver<A: Stream<Item = SocketAddr> + Unp
             .instrument(debug_span!("read_metainfo_from_peer", ?addr))
             .await
             .with_context(|| format!("error reading metainfo from {addr}"));
-            drop(token);
             ret
         }
     };
 
-    let mut unordered = FuturesUnordered::new();
-
-    for a in initial_addrs {
-        seen.insert(a);
-        unordered.push(read_info_guarded(a));
-    }
+    let mut queues = MetadataPeerQueues::new();
 
     let mut addrs_completed = false;
 
     loop {
-        if addrs_completed && unordered.is_empty() {
+        queues.promote_pending(&read_info_guarded);
+
+        while !initial_addrs_completed && queues.can_accept() {
+            let Some(addr) = initial_addrs.pop_front() else {
+                initial_addrs_completed = true;
+                break;
+            };
+            match admit_metadata_peer(&mut seen, addr) {
+                CandidateAdmission::Admitted => {
+                    debug_assert!(queues.enqueue(addr, &read_info_guarded));
+                }
+                CandidateAdmission::Duplicate => {}
+                CandidateAdmission::Untracked => {
+                    if !candidate_limit_logged {
+                        debug!(
+                            limit = MAX_METADATA_PEER_CANDIDATES,
+                            "metadata peer deduplication limit reached; continuing without tracking new candidates"
+                        );
+                        candidate_limit_logged = true;
+                    }
+                    debug_assert!(queues.enqueue(addr, &read_info_guarded));
+                }
+            }
+        }
+
+        if initial_addrs_completed && addrs_completed && queues.is_empty() {
             return ReadMetainfoResult::ChannelClosed { seen };
         }
 
         tokio::select! {
-            done = unordered.next(), if !unordered.is_empty() => {
+            done = queues.next(), if !queues.active.is_empty() => {
                 match done {
                     Some(Ok((info, info_bytes))) => return ReadMetainfoResult::Found { info, info_bytes, seen, rx: addrs },
                     Some(Err(e)) => {
@@ -91,11 +192,26 @@ pub async fn read_metainfo_from_peer_receiver<A: Stream<Item = SocketAddr> + Unp
                 }
             }
 
-            next_addr = addrs.next(), if !addrs_completed => {
+            next_addr = addrs.next(), if initial_addrs_completed
+                && !addrs_completed
+                && queues.can_accept() => {
                 match next_addr {
                     Some(addr) => {
-                        if seen.insert(addr) {
-                            unordered.push(read_info_guarded(addr));
+                        match admit_metadata_peer(&mut seen, addr) {
+                            CandidateAdmission::Admitted => {
+                                debug_assert!(queues.enqueue(addr, &read_info_guarded));
+                            }
+                            CandidateAdmission::Duplicate => {}
+                            CandidateAdmission::Untracked => {
+                                if !candidate_limit_logged {
+                                    debug!(
+                                        limit = MAX_METADATA_PEER_CANDIDATES,
+                                        "metadata peer deduplication limit reached; continuing without tracking new candidates"
+                                    );
+                                    candidate_limit_logged = true;
+                                }
+                                debug_assert!(queues.enqueue(addr, &read_info_guarded));
+                            }
                         }
                         continue;
                     },
@@ -121,6 +237,40 @@ mod tests {
 
     static LOG_INIT: Once = Once::new();
 
+    #[test]
+    fn production_metadata_queues_close_at_exact_boundaries() {
+        let mut queues = MetadataPeerQueues::new();
+        let make_future = |_| std::future::pending::<()>();
+        for port in 1..=(MAX_CONCURRENT_METADATA_PEERS + MAX_PENDING_METADATA_PEERS) {
+            let addr = SocketAddr::from(([127, 0, 0, 1], port as u16));
+            assert!(queues.enqueue(addr, &make_future));
+        }
+        assert_eq!(queues.active.len(), MAX_CONCURRENT_METADATA_PEERS);
+        assert_eq!(queues.pending.len(), MAX_PENDING_METADATA_PEERS);
+        assert!(!queues.can_accept());
+        assert!(!queues.enqueue(SocketAddr::from(([127, 0, 0, 2], 1)), &make_future));
+
+        let mut seen = HashSet::new();
+        for port in 1..=MAX_METADATA_PEER_CANDIDATES {
+            let addr = SocketAddr::from(([127, 0, 0, 1], port as u16));
+            assert_eq!(
+                admit_metadata_peer(&mut seen, addr),
+                CandidateAdmission::Admitted
+            );
+        }
+        let duplicate = SocketAddr::from(([127, 0, 0, 1], 1));
+        assert_eq!(
+            admit_metadata_peer(&mut seen, duplicate),
+            CandidateAdmission::Duplicate
+        );
+        let overflow = SocketAddr::from(([127, 0, 0, 2], 1));
+        assert_eq!(
+            admit_metadata_peer(&mut seen, overflow),
+            CandidateAdmission::Untracked
+        );
+        assert_eq!(seen.len(), MAX_METADATA_PEER_CANDIDATES);
+    }
+
     fn init_logging() {
         #[allow(unused_must_use)]
         LOG_INIT.call_once(|| {
diff --git a/crates/librqbit_lsd/src/lib.rs b/crates/librqbit_lsd/src/lib.rs
index d66583a1..a3de272e 100644
--- a/crates/librqbit_lsd/src/lib.rs
+++ b/crates/librqbit_lsd/src/lib.rs
@@ -14,7 +14,7 @@ use futures::Stream;
 use librqbit_core::{Id20, spawn_utils::spawn_with_cancel};
 use librqbit_dualstack_sockets::{BindDevice, MulticastUdpSocket};
 use parking_lot::RwLock;
-use tokio::sync::mpsc::{UnboundedReceiver, UnboundedSender, unbounded_channel};
+use tokio::sync::mpsc::{Receiver, Sender, channel};
 use tokio_util::sync::CancellationToken;
 use tracing::{debug, debug_span, trace};
 
@@ -28,6 +28,11 @@ const LSD_IPV6: SocketAddrV6 = SocketAddrV6::new(
 );
 
 const RATE_LIMIT_PERIOD: Duration = Duration::from_secs(1);
+const MAX_PENDING_LSD_PEERS: usize = 256;
+
+fn peer_channel() -> (Sender<SocketAddr>, Receiver<SocketAddr>) {
+    channel(MAX_PENDING_LSD_PEERS)
+}
 
 #[derive(Default)]
 struct RateLimiter {
@@ -56,7 +61,8 @@ impl RateLimiter {
 }
 
 struct Announce {
-    tx: UnboundedSender<SocketAddr>,
+    id: u64,
+    tx: Sender<SocketAddr>,
     port: Option<u16>,
     last_reply_ipv4: RateLimiter,
     last_reply_ipv6: RateLimiter,
@@ -65,10 +71,43 @@ struct Announce {
 struct LocalServiceDiscoveryInner {
     socket: MulticastUdpSocket,
     cookie: u32,
+    next_announce_id: AtomicU64,
     cancel_token: CancellationToken,
     receivers: RwLock<HashMap<Id20, Announce>>,
 }
 
+struct AnnounceLifecycle {
+    info_hash: Id20,
+    announce_id: u64,
+    cancel_token: CancellationToken,
+}
+
+impl AnnounceLifecycle {
+    fn finish(&self, receivers: &RwLock<HashMap<Id20, Announce>>) {
+        self.cancel_token.cancel();
+        let mut receivers = receivers.write();
+        if receivers
+            .get(&self.info_hash)
+            .is_some_and(|announce| announce.id == self.announce_id)
+        {
+            receivers.remove(&self.info_hash);
+        }
+    }
+}
+
+fn spawn_announce_task<E: std::fmt::Display + Send + 'static>(
+    lifecycle: &AnnounceLifecycle,
+    port: u16,
+    future: impl std::future::Future<Output = Result<(), E>> + Send + 'static,
+) -> tokio::task::JoinHandle<()> {
+    spawn_with_cancel(
+        debug_span!(parent: None, "lsd-announce", info_hash=?lifecycle.info_hash, port),
+        "lsd-announce",
+        lifecycle.cancel_token.clone(),
+        future,
+    )
+}
+
 #[derive(Clone)]
 pub struct LocalServiceDiscovery {
     inner: Arc<LocalServiceDiscoveryInner>,
@@ -97,6 +136,7 @@ impl LocalServiceDiscovery {
             inner: Arc::new(LocalServiceDiscoveryInner {
                 socket,
                 cookie,
+                next_announce_id: AtomicU64::new(0),
                 cancel_token: opts.cancel_token.clone(),
                 receivers: Default::default(),
             }),
@@ -172,7 +212,9 @@ cookie: {cookie}\r
             let mut addr = addr;
             addr.set_port(bts.port);
 
-            return_if_none!(announce.tx.send(addr).ok());
+            if let Err(error) = announce.tx.try_send(addr) {
+                trace!(?addr, %error, "dropping LSD peer because its result queue is unavailable");
+            }
 
             let announce_port = return_if_none!(announce.port);
 
@@ -237,11 +279,18 @@ cookie: {cookie}\r
         // 1. Periodically announce the torrent.
         // 2. Stream back the results from received messages.
 
-        let (tx, rx) = unbounded_channel::<SocketAddr>();
+        let (tx, rx) = peer_channel();
+        let announce_id = self.inner.next_announce_id.fetch_add(1, Ordering::Relaxed);
+        let cancel_token = self.inner.cancel_token.child_token();
+        let lifecycle = AnnounceLifecycle {
+            info_hash,
+            announce_id,
+            cancel_token,
+        };
 
         struct AddrStream {
-            info_hash: Id20,
-            rx: UnboundedReceiver<SocketAddr>,
+            lifecycle: AnnounceLifecycle,
+            rx: Receiver<SocketAddr>,
             lsd: LocalServiceDiscovery,
         }
 
@@ -258,13 +307,14 @@ cookie: {cookie}\r
 
         impl Drop for AddrStream {
             fn drop(&mut self) {
-                let _ = self.lsd.inner.receivers.write().remove(&self.info_hash);
+                self.lifecycle.finish(&self.lsd.inner.receivers);
             }
         }
 
         self.inner.receivers.write().insert(
             info_hash,
             Announce {
+                id: lifecycle.announce_id,
                 tx,
                 port: announce_port,
                 last_reply_ipv4: Default::default(),
@@ -273,18 +323,16 @@ cookie: {cookie}\r
         );
 
         if let Some(announce_port) = announce_port {
-            let cancel_token = self.inner.cancel_token.child_token();
-            spawn_with_cancel(
-                debug_span!(parent: None, "lsd-announce", ?info_hash, port=announce_port),
-                "lsd-announce",
-                cancel_token,
+            spawn_announce_task(
+                &lifecycle,
+                announce_port,
                 self.clone()
                     .task_announce_periodically(info_hash, announce_port),
             );
         }
 
         AddrStream {
-            info_hash,
+            lifecycle,
             rx,
             lsd: self.clone(),
         }
@@ -347,3 +395,63 @@ fn try_parse_bt_search<'a: 'h, 'h>(
         _ => anyhow::bail!("expecting BT-SEARCH"),
     }
 }
+
+#[cfg(test)]
+mod queue_budget_tests {
+    use super::*;
+
+    fn announce(id: u64, tx: Sender<SocketAddr>) -> Announce {
+        Announce {
+            id,
+            tx,
+            port: None,
+            last_reply_ipv4: Default::default(),
+            last_reply_ipv6: Default::default(),
+        }
+    }
+
+    #[tokio::test]
+    async fn production_queue_and_announce_lifecycle_are_bounded() {
+        let addr = SocketAddr::from(([127, 0, 0, 1], 6881));
+        let (tx, _rx) = peer_channel();
+        for _ in 0..MAX_PENDING_LSD_PEERS {
+            tx.try_send(addr).unwrap();
+        }
+        assert!(matches!(
+            tx.try_send(addr),
+            Err(tokio::sync::mpsc::error::TrySendError::Full(_))
+        ));
+
+        let info_hash = Id20::new([1; 20]);
+        let registrations = RwLock::new(HashMap::new());
+        registrations
+            .write()
+            .insert(info_hash, announce(2, tx.clone()));
+
+        let stale = AnnounceLifecycle {
+            info_hash,
+            announce_id: 1,
+            cancel_token: CancellationToken::new(),
+        };
+        stale.finish(&registrations);
+        assert!(stale.cancel_token.is_cancelled());
+        assert_eq!(registrations.read().get(&info_hash).unwrap().id, 2);
+
+        let current = AnnounceLifecycle {
+            info_hash,
+            announce_id: 2,
+            cancel_token: CancellationToken::new(),
+        };
+        let task = spawn_announce_task(&current, 6881, async {
+            std::future::pending::<()>().await;
+            Ok::<(), std::convert::Infallible>(())
+        });
+        current.finish(&registrations);
+        tokio::time::timeout(Duration::from_secs(1), task)
+            .await
+            .expect("announce task must observe stream cancellation")
+            .unwrap();
+        assert!(current.cancel_token.is_cancelled());
+        assert!(!registrations.read().contains_key(&info_hash));
+    }
+}
